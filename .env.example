# Copy to `.env` (or `.env.local`) and adjust. Never commit the real `.env`.

# Base URL of the ezServiceHUB WebAPI. Defaults to the `https` launch
# profile in that project's Properties/launchSettings.json for local dev.
VITE_API_BASE_URL=https://localhost:7092
